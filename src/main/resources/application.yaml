spring:
  application:
    name: telegram-bot-s

  datasource:
    url: jdbc:postgresql://localhost:5437/business_platform
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8084